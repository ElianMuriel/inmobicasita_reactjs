# CAMBIOS PARA: gestion/serializers.py
# Modificar ClienteSerializer para incluir user_id

# Reemplazar el ClienteSerializer actual con este:
"""
class ClienteSerializer(serializers.ModelSerializer):
    user_id = serializers.IntegerField(source='user.id', read_only=True)
    
    class Meta:
        model = Cliente
        fields = '__all__'
"""
